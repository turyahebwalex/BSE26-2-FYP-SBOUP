/**
 * SBOUP Design System — Typography Tokens
 */
const typography = {
  fontFamily: "'Inter', system-ui, -apple-system, sans-serif",

  // Font sizes (px)
  size: {
    xs:   '12px',
    sm:   '14px',
    base: '16px',
    lg:   '18px',
    xl:   '20px',
    '2xl': '24px',
    '3xl': '30px',
  },

  // Font weights
  weight: {
    regular:   400,
    medium:    500,
    semibold:  600,
    bold:      700,
  },

  // Line heights
  lineHeight: {
    tight:  1.25,
    normal: 1.5,
    relaxed: 1.75,
  },
};

export default typography;
